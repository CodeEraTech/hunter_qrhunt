<?php declare(strict_types=1);require dirname(__DIR__,2).'/includes/bootstrap.php';require_permission('security.view');$rows=db()->query('SELECT * FROM login_attempts ORDER BY id DESC LIMIT 500')->fetchAll();$pageTitle='Login attempts';$pageSubtitle='Hashed identities—credentials are never logged';require APP_ROOT.'/includes/header.php';?>
<section class="panel"><div class="panel-head"><h2>Recent authentication attempts</h2><a href="<?=e(app_url('/admin/fraud/devices.php'))?>">Devices →</a></div><div class="table-wrap"><table><thead><tr><th>Date</th><th>Identity fingerprint</th><th>IP</th><th>Outcome</th></tr></thead><tbody><?php foreach($rows as $r):?><tr><td><?=e($r['attempted_at'])?></td><td><code><?=e(substr($r['identity_hash'],0,16))?>…</code></td><td><?=e($r['ip_address'])?></td><td><span class="badge <?=$r['successful']?'success':'failed'?>"><?=$r['successful']?'SUCCESS':'FAILED'?></span></td></tr><?php endforeach?></tbody></table></div></section><?php require APP_ROOT.'/includes/footer.php';

